[phases.setup]
nixPkgs = ['nodejs_20', 'npm-9_x']

[phases.install]  
cmds = ['npm ci']

[phases.build]
cmds = [
  'mkdir -p data',
  'npm run build',
  'npx prisma generate',
  'npx prisma migrate deploy'
]

[start]
cmd = 'npm start'